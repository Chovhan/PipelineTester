tasks.register("execTest") {
    dependsOn pullRep
    exec {
        workingDir 'batFiles'
        commandLine "cmd", "/c", "executePython.bat"
    }
}

task pullRep(type: Exec) {
    commandLine "git", "pull", "https://github.com/Chovhan/PipelineTester.git", "master"
}